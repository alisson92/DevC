#include <stdlib.h>
#include <stdio.h>
 int main()
  {
  	int num, maiornum, numpar, cont;
  	maiornum = 0;
  	
	for (cont=1;cont<=5;cont++)
  	
		{
		printf("Digite um numero: ");
  		scanf(" %d", &num);
  		if (num>maiornum)
  			{
  				maiornum=num;	
			}
				if (num%2=0);
				{
				numpar=num+numpar
				}
    	}
   		printf("O maior numero lido foi = %d\n", maiornum);
   		return 0;
	}
 
